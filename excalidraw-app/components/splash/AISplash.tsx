import type { ExcalidrawImperativeAPI } from "@excalidraw/excalidraw/types";

/**
 * Variant B ("ai"): prominent "Generate with AI" entry into the
 * text-to-diagram dialog. Rendered as the first `WelcomeScreen.Center.Menu`
 * entry.
 */
export const AISplash = (_props: {
  excalidrawAPI: ExcalidrawImperativeAPI | null;
}) => {
  return null;
};
