import type { ExcalidrawImperativeAPI } from "@excalidraw/excalidraw/types";

/**
 * Variant A ("templates"): "Start from a template" welcome-screen menu item
 * opening a templates dialog. Rendered as the first `WelcomeScreen.Center.Menu`
 * entry.
 */
export const TemplatesSplash = (_props: {
  excalidrawAPI: ExcalidrawImperativeAPI | null;
}) => {
  return null;
};
